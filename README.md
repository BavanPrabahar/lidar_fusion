This repository contains files to fuse scan data from two LiDARs.

*The LiDARs are placed at diagonally opposite corners of the rectangular mobile robot.

*The sensors have no direct line of sight between them.

*The fused output is treated as if the LiDAR is placed at the center.

*The goal is to generate a single scan data stream from both sensors.
